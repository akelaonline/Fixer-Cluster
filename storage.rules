rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Por ahora, solo Cloud Functions (Admin SDK) accede al bucket.
      // Bloqueamos acceso desde SDKs cliente hasta definir auth.
      allow read, write: if false;
    }
  }
}
