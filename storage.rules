rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Dev/emulator: open. Tighten before prod.
    match /{allPaths=**} {
      allow read, write: if request.time < timestamp.date(2100, 1, 1);
    }
  }
}
