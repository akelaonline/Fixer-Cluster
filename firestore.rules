rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Por ahora, solo Cloud Functions (Admin SDK) accede a Firestore.
      // Bloqueamos todo acceso desde SDKs cliente hasta definir auth.
      allow read, write: if false;
    }
  }
}
